# Kernel bug causing high resolution timers (hrtimer) to get lost, or even a spurious hard lockup.

- Comments
- Helpful
- Get notified when this content is updated.FollowPlease wait...


## Get notified when this content is updated.

Please wait...


# Kernel bug causing high resolution timers (hrtimer) to get lost, or even a spurious hard lockup.

- English
- Machine Translations
- Chinese
- Chinese, Traditional
- French
- German
- Italian
- Japanese
- Korean
- Portuguese
- Russian
- Spanish


## Environment

- Red Hat Enterprise Linux 7.5
- Red Hat Enterprise Linux 7.4


## Issue

- High resolution timer (hrtimer) gets lost / missed / not triggered.
- Hard lockup reported (optionally panic) while there's no apparent actual lockup.Hard lockup in userspaceHard lockup with interrupts enabled

- Hard lockup in userspace
- Hard lockup with interrupts enabled

NOTE: Only RHEL 7.4 and 7.5 are affected by this, as only those kernel versions contain the patch that introduced the bug.


## Resolution

The issue is fixed inRHEL 7.6. Please installkernel-3.10.0-957.el7or later, as per erratumRHSA-2018:3083.

`kernel-3.10.0-957.el7`ForRHEL 7.4 (L4S), installkernel-3.10.0-693.81.1.el7or later, as per erratumRHSA-2020:5430.

`kernel-3.10.0-693.81.1.el7`In case you would encounter similar issue even with fixed kernel version, please open a support case.


## Root Cause

There was a race condition bug, which could cause virtually any high resolution timer (hrtimer), including thewatchdog_timer_fn()used for hard lockup detection, to get forgotten/lost (hence never executed afterwards, unless explicitly restarted), due to inconsistency in the hrtimer red-black tree created during the race window.

The problem is well explained in the fixing commit message:


```
commit 1f71addd34f4c442bec7d7c749acc1beb58126f2   // upstream commit hash
Author: Thomas Gleixner <tglx@linutronix.de>
Date:   Tue Apr 24 21:22:18 2018 +0200

    tick/sched: Do not mess with an enqueued hrtimer

    Kaike reported that in tests rdma hrtimers occasionaly stopped working. He
    did great debugging, which provided enough context to decode the problem.

    CPU 3                                        CPU 2

    idle
    start sched_timer expires = 712171000000
     queue->next = sched_timer
                                                start rdmavt timer. expires = 712172915662
                                                lock(baseof(CPU3))
    tick_nohz_stop_tick()
    tick = 716767000000                         timerqueue_add(tmr)

    hrtimer_set_expires(sched_timer, tick);
      sched_timer->expires = 716767000000  <---- FAIL
                                                 if (tmr->expires < queue->next->expires)
    hrtimer_start(sched_timer)                        queue->next = tmr;
    lock(baseof(CPU3))
                                                 unlock(baseof(CPU3))
    timerqueue_remove()
    timerqueue_add()

    ts->sched_timer is queued and queue->next is pointing to it, but then
    ts->sched_timer.expires is modified.

    This not only corrupts the ordering of the timerqueue RB tree, it also
    makes CPU2 see the new expiry time of timerqueue->next->expires when
    checking whether timerqueue->next needs to be updated. So CPU2 sees that
    the rdma timer is earlier than timerqueue->next and sets the rdma timer as
    new next.

    Depending on whether it had also seen the new time at RB tree enqueue, it
    might have queued the rdma timer at the wrong place and then after removing
    the sched_timer the RB tree is completely hosed.

    The problem was introduced with a commit which tried to solve inconsistency
    between the hrtimer in the tick_sched data and the underlying hardware
    clockevent. It split out hrtimer_set_expires() to store the new tick time
    in both the NOHZ and the NOHZ + HIGHRES case, but missed the fact that in
    the NOHZ + HIGHRES case the hrtimer might still be queued.

    Use hrtimer_start(timer, tick...) for the NOHZ + HIGHRES case which sets
    timer->expires after canceling the timer and move the hrtimer_set_expires()
    invocation into the NOHZ only code path which is not affected as it merily
    uses the hrtimer as next event storage so code pathes can be shared with
    the NOHZ + HIGHRES case.

    Fixes: d4af6d933ccf ("nohz: Fix spurious warning when hrtimer and clockevent get out of sync")
```

`commit 1f71addd34f4c442bec7d7c749acc1beb58126f2   // upstream commit hash
Author: Thomas Gleixner <tglx@linutronix.de>
Date:   Tue Apr 24 21:22:18 2018 +0200

    tick/sched: Do not mess with an enqueued hrtimer

    Kaike reported that in tests rdma hrtimers occasionaly stopped working. He
    did great debugging, which provided enough context to decode the problem.

    CPU 3                                        CPU 2

    idle
    start sched_timer expires = 712171000000
     queue->next = sched_timer
                                                start rdmavt timer. expires = 712172915662
                                                lock(baseof(CPU3))
    tick_nohz_stop_tick()
    tick = 716767000000                         timerqueue_add(tmr)

    hrtimer_set_expires(sched_timer, tick);
      sched_timer->expires = 716767000000  <---- FAIL
                                                 if (tmr->expires < queue->next->expires)
    hrtimer_start(sched_timer)                        queue->next = tmr;
    lock(baseof(CPU3))
                                                 unlock(baseof(CPU3))
    timerqueue_remove()
    timerqueue_add()

    ts->sched_timer is queued and queue->next is pointing to it, but then
    ts->sched_timer.expires is modified.

    This not only corrupts the ordering of the timerqueue RB tree, it also
    makes CPU2 see the new expiry time of timerqueue->next->expires when
    checking whether timerqueue->next needs to be updated. So CPU2 sees that
    the rdma timer is earlier than timerqueue->next and sets the rdma timer as
    new next.

    Depending on whether it had also seen the new time at RB tree enqueue, it
    might have queued the rdma timer at the wrong place and then after removing
    the sched_timer the RB tree is completely hosed.

    The problem was introduced with a commit which tried to solve inconsistency
    between the hrtimer in the tick_sched data and the underlying hardware
    clockevent. It split out hrtimer_set_expires() to store the new tick time
    in both the NOHZ and the NOHZ + HIGHRES case, but missed the fact that in
    the NOHZ + HIGHRES case the hrtimer might still be queued.

    Use hrtimer_start(timer, tick...) for the NOHZ + HIGHRES case which sets
    timer->expires after canceling the timer and move the hrtimer_set_expires()
    invocation into the NOHZ only code path which is not affected as it merily
    uses the hrtimer as next event storage so code pathes can be shared with
    the NOHZ + HIGHRES case.

    Fixes: d4af6d933ccf ("nohz: Fix spurious warning when hrtimer and clockevent get out of sync")`
## Diagnostic Steps

The race condition can generally cause hrtimers to get lost/forgotten, due to rb-tree corruption during the race window. The hrtimers, however, are still present in the tree,even while crash does not show them via "timer -r". The hrtimer_clock_base rbtree needs to be checked manually:


```
crash> timer -r -C 21
CPU: 21  HRTIMER_CPU_BASE: ffff9ecd3e0939e0
  CLOCK: 0  HRTIMER_CLOCK_BASE: ffff9ecd3e093a20  [ktime_get]
       CURRENT    
  4083414375000000
     SOFTEXPIRES         EXPIRES          HRTIMER           FUNCTION    
  4083413994000000  4083413994000000  ffff9ecd3e093f40  ffffffffb9b04690  <tick_sched_timer>
  4083414007192887  4083414008192887  ffff9d511c551ac0  ffffffffb9ee4670  <ehci_hrtimer_func>
  4083414052511639  4083414052711638  ffff9d94c43c3a20  ffffffffb9ac19a0  <hrtimer_wakeup>
  4083416379254914  4083416379304914  ffff9d998bb57e88  ffffffffb9ac19a0  <hrtimer_wakeup>
  4083418552990529  4083418553040529  ffff9e5906e93d38  ffffffffb9ac19a0  <hrtimer_wakeup>
  4083699183536333  4083699183586333  ffff9e59ffb47d38  ffffffffb9ac19a0  <hrtimer_wakeup>
---8<---

-- Notice _6_ hrtimers outputted by "timer -r". 
-- Next check the hrtimer_clock_base red-black tree:

crash> hrtimer_clock_base.active ffff9ecd3e093a20
  active = {
    head = {
      rb_node = 0xffff9d998bb57e88
    }, 
    next = 0xffff9ecd3e093f40
  }

crash> tree -t rbtree -N 0xffff9d998bb57e88
ffff9d998bb57e88
ffff9ecd3e094120
ffff9e58f02e3e88
ffff9d511c551ac0
ffff9ecd3e093f40
ffff9d94c43c3a20
ffff9e59ffb47d38
ffff9e5906e93d38

-- Notice _8_ hrtimers in the rbtree (crash's "timer -r" only goes in-order from ->next pointer)
-- Next check whether ->next is the leftmost node in the rbtree:

crash> rb_node 0xffff9d998bb57e88     <<-- root
struct rb_node {
  __rb_parent_color = 0x1, 
  rb_right = 0xffff9e59ffb47d38, 
  rb_left = 0xffff9ecd3e094120        <<-- go left
}
crash> rb_node 0xffff9ecd3e094120
struct rb_node {
  __rb_parent_color = 0xffff9d998bb57e88, 
  rb_right = 0xffff9d511c551ac0, 
  rb_left = 0xffff9e58f02e3e88        <<-- go left
}
crash> rb_node 0xffff9e58f02e3e88
struct rb_node {
  __rb_parent_color = 0xffff9ecd3e094121, 
  rb_right = 0x0,                      <<--,
  rb_left = 0x0                        <<--'-- leaf node (leftmost)
}

-- Notice that ->next == 0xffff9ecd3e093f40, but leftmost == 0xffff9e58f02e3e88, hence [->next != leftmost], which shows that it's broken.
```

`crash> timer -r -C 21
CPU: 21  HRTIMER_CPU_BASE: ffff9ecd3e0939e0
  CLOCK: 0  HRTIMER_CLOCK_BASE: ffff9ecd3e093a20  [ktime_get]
       CURRENT    
  4083414375000000
     SOFTEXPIRES         EXPIRES          HRTIMER           FUNCTION    
  4083413994000000  4083413994000000  ffff9ecd3e093f40  ffffffffb9b04690  <tick_sched_timer>
  4083414007192887  4083414008192887  ffff9d511c551ac0  ffffffffb9ee4670  <ehci_hrtimer_func>
  4083414052511639  4083414052711638  ffff9d94c43c3a20  ffffffffb9ac19a0  <hrtimer_wakeup>
  4083416379254914  4083416379304914  ffff9d998bb57e88  ffffffffb9ac19a0  <hrtimer_wakeup>
  4083418552990529  4083418553040529  ffff9e5906e93d38  ffffffffb9ac19a0  <hrtimer_wakeup>
  4083699183536333  4083699183586333  ffff9e59ffb47d38  ffffffffb9ac19a0  <hrtimer_wakeup>
---8<---

-- Notice _6_ hrtimers outputted by "timer -r". 
-- Next check the hrtimer_clock_base red-black tree:

crash> hrtimer_clock_base.active ffff9ecd3e093a20
  active = {
    head = {
      rb_node = 0xffff9d998bb57e88
    }, 
    next = 0xffff9ecd3e093f40
  }

crash> tree -t rbtree -N 0xffff9d998bb57e88
ffff9d998bb57e88
ffff9ecd3e094120
ffff9e58f02e3e88
ffff9d511c551ac0
ffff9ecd3e093f40
ffff9d94c43c3a20
ffff9e59ffb47d38
ffff9e5906e93d38

-- Notice _8_ hrtimers in the rbtree (crash's "timer -r" only goes in-order from ->next pointer)
-- Next check whether ->next is the leftmost node in the rbtree:

crash> rb_node 0xffff9d998bb57e88     <<-- root
struct rb_node {
  __rb_parent_color = 0x1, 
  rb_right = 0xffff9e59ffb47d38, 
  rb_left = 0xffff9ecd3e094120        <<-- go left
}
crash> rb_node 0xffff9ecd3e094120
struct rb_node {
  __rb_parent_color = 0xffff9d998bb57e88, 
  rb_right = 0xffff9d511c551ac0, 
  rb_left = 0xffff9e58f02e3e88        <<-- go left
}
crash> rb_node 0xffff9e58f02e3e88
struct rb_node {
  __rb_parent_color = 0xffff9ecd3e094121, 
  rb_right = 0x0,                      <<--,
  rb_left = 0x0                        <<--'-- leaf node (leftmost)
}

-- Notice that ->next == 0xffff9ecd3e093f40, but leftmost == 0xffff9e58f02e3e88, hence [->next != leftmost], which shows that it's broken.`- Product(s)
- Red Hat Enterprise Linux

- Component
- kernel

- Category
- Troubleshoot

- Tags
- kernel

This solution is part of Red Hat’s fast-track publication program, providing a huge library of solutions that Red Hat engineers have created while supporting our customers. To give you the knowledge you need the instant it becomes available, these articles may be presented in a raw and unedited form.


## Was this helpful?


## Get notified when this content is updated

Please wait...


## Comments

